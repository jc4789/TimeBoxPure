# 目的

`timerplan.md` の「段階7: 任意の隣接改善—SSG拡張デチューン」を、指定資料の解釈に従って実装する。

# 制約

- Geminiの解釈を確定済みの前提として扱い、否定・再検証しない。
- オーバーフローは `gemini on overflow.txt` を正とする。
- PMDソフトウェアLFOの不足情報だけを `pmd soft LFO info.txt` で補う。
- YM2608 SSG資料はハードウェア側専用であり、PMD側の根拠にしない。
- 指定資料以外の外部情報を使わない。
- テストまたは診断を作成・実行しない。
- 明示範囲外の改善を行わない。

# 計画

- [x] `timerplan.md` 全文と指定資料を読む。
- [x] 段階7に関係する製品コードを全文読む。
- [x] 変更対象ファイルの許可一覧を確定する。
- [x] SSG拡張デチューンを実装する。
- [x] 差分を資料、法則、許可範囲に照合する。

# 確認済み

- `context-drift-guard` と `Plan-implimentation-guidelines` の全文を再読した。
- `timerplan.md`、指定されたShift_JISテキスト4本、YM2608 SSG画像4枚を読了した。
- `#Detune Extend` はG/H/I先頭の `DX1` と同義、既定は通常、`DX` はパート別である。
- 拡張補正は高音ほど量を小さくし、非ゼロ値は最低1単位ずらす。
- オーバーフローはYM2608ハードウェア概念であり、一回のIRQがPMDの一内部クロックを進める。
- 関係コードの全文と、SSG周期への最終適用経路を読了した。
- 通常モードは既存挙動を維持し、拡張モードだけをPMD層で解決する。
- 変更許可一覧は `MmlParser.kt`、`MmlCompiler.kt`、`PmdPerformanceLaws.kt`、`CompiledOpnaSong.kt`、`CompiledOpnaTimeline.kt`、`CompiledOpnaPlayer.kt`、`PmdPerformanceState.kt`。
- `#Detune Normal/Extend` とSSGパート別 `DX0/1` を追加した。
- 各SSGノートのモードを基本型配列で製品再生経路へ渡した。
- 拡張時だけ静的デチューンと音程LFOをPMDオクターブごとに縮小し、非ゼロ値の最低1単位を維持した。
- タイマー、内蔵LFO、ポルタメント補間、物理SSG実装、ミキサーは変更していない。
- コンパイル済み曲と時系列の配列生成、並べ替え、再生時の引き渡しを目視で照合した。
- 追加した再生時処理は整数演算と既存の周期変換だけであり、新しい割り当てを行わない。
- 差分は許可した製品コード7ファイルだけで、外部資産、プラットフォーム層、生成物を変更していない。

# 却下

- なし。

# 未確認

- 人間による聴取。今回はテスト・診断禁止のため実施しない。

# 次

実装と静的な目視照合は完了。ビルド、テスト、診断、聴取は指示どおり実施しない。
