# タイマーB統合計画

## 1. 目的

手続き型クリーンルームYM2608音源層と、独立したPMD系MML・演奏モデル層の境界を保ったまま、タイマーBを共通の時間源として導入する。

この計画の主眼は次の三点である。

1. YM2608資料に基づくタイマーBの周期と必要な制御論理を、物理音源層へ直接的な型として追加する。
2. PMDの `#Timer` / `T` と `#Tempo` / `t` を区別し、音符進行、通常速度のソフトウェアLFO、通常速度のソフトウェア包絡、PMDクロック指定の遅延を同じタイマーB時間軸へ接続する。
3. 現在動いている製品経路、無割り当て描画、二層所有境界、内蔵LFOの独立性を維持する。

関連するエンジン法則は、「音響構造には、手続き型クリーンルームYM2608音源層と、独立したクリーンルームPMD系MML・演奏モデル層がある」である。

## 2. 絶対的な範囲制限

### 許可するもの

- 資料のタイマーB周期式。
- タイマーBのプリセット、読み込み、開始、停止、自動再読み込み、オーバーフロー通知、通知許可、通知消費に必要な状態遷移。
- 上記を名前付きの直接操作として表現すること。
- タイマーB通知をPMD演奏クロックとして使用すること。
- PMDのタイミング構文、通常／拡張LFO速度、通常／拡張包絡速度との接続。
- タイマーB統合に直接必要なコンパイル済み曲、時系列、再生器の変更。

### 一切許可しないもの

- タイマーA。型、定数、状態、分岐、将来用の空実装も追加しない。
- バス待機。待機時間、ビジー状態、書き込み待ち列も追加しない。
- レジスター窓口。アドレス指定、汎用読み書き、レジスター配列、ビットフィールド公開、疑似書き込み命令も追加しない。
- 資料上の `$26`、`$27` などをコード上のインターフェースにすること。
- CSM、原始レジスター列、PMDバイナリー再生。
- 画面、プラットフォーム、ミキサー、音量、EQ、フィルター、共鳴、クリップ、出力経路の変更。
- 楽曲データを音量やヘッドルーム調整のために変更すること。
- 外部音声素材、外部設定、追加依存関係。
- 恒久的な検査、記録、追跡、比較、スナップショット用の本番機構。
- ユーザーの別途明示的な許可なしでの検査コード作成・実行。

レジスター資料に書かれている論理は、必要であれば使う。禁止するのはレジスター窓口であって、その背後にある正しいタイマーB動作ではない。

## 3. 確認した資料上の事実

### 3.1 YM2608タイマーB

- タイマーBは8ビット値 `NB=0..255` を使う。
- 周期は次の式である。

```text
タイマーB周期 = 1152 × (256 - NB) / φM 秒
```

- 現行音源のマスタークロック `φM` は `8,000,000Hz` である。
- 8MHz時の1段階は144マイクロ秒である。
- 制御資料には、読み込みによる開始、読み込み解除による停止、オーバーフロー通知の許可、オーバーフローフラグ、フラグ消去が記載されている。
- この計画では、それらのうち実行時動作に必要な論理を直接的な名前付き操作として表す。アドレスやビット位置は表さない。

### 3.2 PMDテンポと音長

- `#Timer` は初期タイマーB値を直接指定し、範囲は `0..250`。
- `T` は曲中でタイマーB値を直接指定し、範囲は `0..255`。相対指定もある。
- `#Tempo` と `t` は内部クロック 48 が１分間に何回になるかを指定します。
	内部クロック 48 は、デフォルトでは２分音符ですが、#Zenlen コマンド
	または C コマンドで変更されます。
- `t` の範囲は `18..255`。
- タイミング値は全パートで共有される。
- `#Tempo` / `#Timer` はパートG先頭の `t` / `T` として発行され、`#Zenlen` も既定値以外ではパートG先頭の `C` として発行される。したがって、ヘッダー指定と曲中指定を別々の時間状態として扱ってはならない。
- `C` / `#Zenlen` は全音符の内部クロック数であり、既定値は96、範囲は `1..255`。
- 通常音長 `n` はクロック数ではなく音符の分母である。内部クロック長は `C ÷ n` なので、通常音長 `n` は `C` の約数でなければならない。
- `%n` は分母ではなく、`n` 内部クロックを直接指定する。`C96` で四分音符が24クロックとなるため、五連符のように割り切れない長さは `%4` と `%5` のように余りを配分して近似する。
- 音長省略時は、そのパートの `l` 既定音長を使う。`C` を指定した時点で `l` は4へ戻る。
- 付点は基礎長へ半分、四分の一、八分の一を順に加える。資料の確定例は `c2.. = c2&c4&c8` である。
- 音符、休符、ポルタメントは同じ通常音長、直接クロック長、付点規則を使う。
- `C96` では内部48クロックが二分音符、`C192` では四分音符に相当する。
- ポルタメントは総内部長255以下という固有制限がある。通常音符の255超制限はW/S使用時など条件付きなので、全音符へ一律適用してはならない。
- `Q0..8` は音長に対する8分率、`Q%0..255` は256分率で基礎キーオフ位置を決める。`Q0` は最後まで切らない。
- `q` は `Q` で決めた位置からPMD内部クロック数を引く。`q2` は直接2クロック、`ql8` はその時点の通常8分音符長、つまり `C ÷ 8` クロックとして扱う。
- `q` は短縮量の乱数範囲と最低発音長も持つ。最低値0は最低1クロックを意味する。
- 資料の確定例では `C96 Q4 q2 c4` は、四分音符24クロックの半分12から2を引き、10クロック目でキーオフする。

### 3.3 PMD版Bad Appleの実データ

`D:\Programes\ym2608-info\zun_music\TH4 Bad Apple!! MML Dump.txt` をタイミング構文の実曲資料として扱う。

- ヘッダーは `#Title Bad Apple!!`、作曲・編曲はZUN（太田）と記載されている。
- 大文字 `T` と `#Timer` は存在せず、音楽テンポ指定は小文字 `t` である。
- A～E、G、Hパートは `t80` を使用する。Gパートだけは先頭で `t70` を指定した直後、長さを消費する音符や休符より前に `t80` を指定している。
- A～Jパートの先頭には `MXA1 MXB1` があり、音符のないF、I、Jパートにも置かれている。これは資料にある拡張LFO速度の実曲使用例である。
- この曲は `%9`、`%18`、`%36`、`%66`、`%72`、`%129`、`%144`、`%192`、`%255` を合計59回使う。直接PMDクロック長は周辺機能ではなく、Bad Appleの音符・休符配置を再現する中心的な時間構文である。
- `t` は全パート共有値なので、各パートに現れる `t80` をパート固有テンポとして複製してはならない。同一時刻の大域指定として統合し、正規化済み原文順で最終値を決める。
- 既定の `C96` では `t80` は二分音符80、四分音符160に相当する。現行製品曲の `#BPM 160.73` はこれに近く、PMDタイマー量子化後の近似値である可能性が高い。この対応は推論として扱い、変換定数の根拠そのものにはしない。
- この資料の音色定義、DT2、リズムパターン、未対応演奏コマンドを、タイマーB計画を理由に実装範囲へ広げない。

### 3.4 PMDソフトウェアLFOと包絡

- 通常速度のソフトウェアLFOはテンポ依存。
- `#LFOSpeed Extend` は対象パートの二つのLFOへ `MXA1 MXB1` を与えることに相当し、テンポ非依存になる。
- 通常速度のソフトウェア包絡はテンポ依存。
- `#EnvelopeSpeed Extend` は対象SSGパートへ `EX1` を与えることに相当し、テンポ非依存になる。
- 現行実装と資料はいずれも拡張速度を約56Hzとしている。
- `#Detune Extend` は、SSGのデチューン／LFO量を高音域で補正し、音域による偏差量の変化を抑える。

### 3.5 YM2608内蔵LFO

- 内蔵LFOはタイマーBとは別の発振器である。
- 8段階の周波数は `3.98, 5.56, 6.02, 6.37, 6.88, 9.63, 48.1, 72.2Hz`。
- PMSはチャンネル単位の音程変調深度、AMSはチャンネル単位の振幅変調深度である。
- 振幅変調は各オペレーターの許可状態にも依存する。
- 現行 `Lfo.kt`、`OpnaLfoLaws.kt`、`Fm4OpVoice.kt` はこの周波数表、PMS、AMS、オペレーター単位の振幅変調許可をすでに表現している。
- タイマーB統合のために内蔵LFOをタイマーB駆動へ変えてはならない。

## 4. 現行コードの確認結果

### 4.1 製品再生経路

```text
SoundPreviewPlayer
  → MmlArrangementScheduler.createPlayer
  → CompiledOpnaTimelineFactory.build
  → CompiledOpnaPlayer.render
  → PmdPerformanceState.prepare
  → OpnaLikeSynthesizer.renderTimelineSegment
  → OPNA物理音源と出力処理
```

- 製品は48kHz、単音、1024フレーム区間で描画する。
- `CompiledOpnaPlayer` がPMD論理状態とイベント解釈を所有する。
- `OpnaLikeSynthesizer` と `OpnaChipState` が物理音源状態を所有する。
- `OpnaLikeSynthesizer` がPMDイベントを解釈したり、PMD状態を所有したりする構造へ戻してはならない。

### 4.2 現行時間軸

- `MmlParser.kt` は初期値に `#BPM`、曲中変更に大文字 `T` を使う。
- 現行の大文字 `T` はタイマーB値ではなく整数BPMとして解釈される。
- `#PMDCLOCK` は四分音符当たりのPMDクロック数として保持される。
- 現行音長は固定の全音符1920刻みを分母で割る。合法性も固定刻みの約数かで判定され、`C` による全音符クロック変更は表現されない。
- 現行の音符・休符・ポルタメントは分母と付点1個の真偽値だけを持つ。`%n` 直接クロック長と複数付点は解析されない。
- 現行 `PmdGateState` は `Q` / `Q%`、生クロック形式の `q`、乱数範囲、最低発音長を持ち、設定時に固定刻みへ解決する。資料にある `ql音長` 形式は解析されない。
- `PmdSampleClock.kt` はBPM、480刻みの四分音符時間、テンポ変更列から全イベントのサンプル境界を事前計算する。
- `CompiledOpnaTimeline.kt` はそのサンプル境界を確定し、実行時は基本型配列のカーソルだけを進める。
- 同一時刻の順序は、大域制御、状態、キーオフ、キーオン、ゼロゲートオフである。

### 4.3 現行PMD変調

- `PmdSoftwareLfo.kt` の通常速度は、BPMと四分音符当たりクロック数から各LFOが独自にサンプル位相を作る。
- `PmdSoftwareEnvelope.kt` も通常速度について独自のサンプル位相を作る。
- したがって、音符時系列、各LFO、各包絡は同じBPMを参照するが、一つの物理タイマーB通知を共有してはいない。
- 拡張速度は約56Hzの独立時間源であり、この性質は維持する。
- PMDクロック指定の内蔵LFO遅延は、現在BPMからフレーム数へ変換され、`Fm4OpVoice` がサンプルごとに減算する。

### 4.4 現行の活動中コード

- 現行製品用Bad Appleは、PMD版から移された時間指定として `#BPM 160.73` と `#PMDCLOCK 24` を使い、曲中の大文字 `T` は使わない。
- PMD版Bad Appleは小文字 `t80` を主値として使い、Gパート先頭の同一時刻に `t70`、続いて `t80` がある。現行の一種類のテンポ命令では、この原意を型として保持できない。
- LOGO曲はユーザーの明示により、設計根拠、互換対象、移行対象、音響採用判定のすべてから除外する。現行コードに残っていても、その構文や音響を守るためにタイマー設計を曲げない。
- `OpnaLikeSynthesizer` には直接的な `OpnaSequencer` 描画経路も残っている。今回の製品MMLタイマーB統合では変更しない。
- 検査コードや検査用基盤は今回の変更候補に含めない。

## 5. 実装前に解決必須の仕様

### 5.1 タイマーBオーバーフローとPMD内部クロックの比率

文章資料とPMD版Bad AppleのMMLだけでは、タイマーBオーバーフロー何回でPMD内部1クロックを進めるかは明記されていない。

8MHz、`NB=0` の最長周期でも、オーバーフロー周波数は次の値になる。

```text
8,000,000 / (1152 × 256) ≒ 27.1267Hz
```

一方、PMDの `t18` が要求する内部クロック周波数は次の値である。

```text
18 × 48 / 60 = 14.4Hz
```



- `t80` をこの規則で8MHzのタイマーBへ近似すると、候補値 `NB=202` で四分音符は約 `160.751` となり、現行の `#BPM 160.73` と近い。
- 下限 `t18` も候補値 `NB=15` で約 `18.009` として8ビット範囲内に収まる。
- これにより、`t18` が一オーバーフロー方式では表せない矛盾は解消する。

ただし、MMLダンプは分周回数や整数丸めを直接記述していない。上記を確認済みPMD動作へ昇格させるには、PMD実装資料による確認、またはこの製品で二分周を採用する明示的判断が必要である。

実装前に、追加の権威あるPMD資料または明示的な製品判断で次を固定する。

- 変換不能な値を診断するか、境界値へ丸めるか。既定方針は診断であり、無言の丸めは行わない。

この決定を一つの名前付きPMD法則として置き、複数箇所へ数値を散らさない。

### 5.2 実行中のプリセット変更

選択資料は、実行中にタイマーB値だけを書き換えたとき、現在進行中の周期を即時再読み込みするか、次回の自動再読み込みから新値を使うかを明示していない。

`T` の曲中変更境界について次を固定する必要がある。

- 変更境界までは旧周期を使う。
- 境界で新しいプリセットを設定する。
- 同時に読み込み直すか、現在周期を完了させるか。
- 同一境界の音符・LFO・包絡更新より前か後か。

既定の計画案は、大域タイミング制御を同一境界の状態・キーイベントより先に適用し、新周期は境界後の次区間から使うことである。読み込み直しの有無だけは資料で確定するまで実装しない。

### 5.3 `C`、直接クロック長、付点の整数規則

資料により通常音長と `%n` の意味は確定したが、次の整数端数規則は明記されていない。

- 奇数クロック長へ付点を加えるとき、各半分を切り捨て、四捨五入、余り持ち越しのどれで処理するか。
- 複数付点で生じる各段階の端数を、段階ごとに丸めるか最後にまとめて丸めるか。

これらはPMD実装資料で確認する。未確認のまま現行の一付点式 `base + base / 2` を複数付点へ反復してPMD法則と呼ばない。

一方、次は資料で確定しているため設計判断にしない。

- 通常音長 `n` は `C ÷ n` クロックであり、`n` は `C` の約数。
- `%n` は `n` クロックそのもの。
- `C96` の2分音符は48クロック。 2分音符 is default in pmd.
- `C` は全パート共有だが、`l` はパート固有の解析状態である。この計画では `C` を記述したパートの `l` だけを4へ戻し、他パートの既定音長を同時に書き換えない。
- 音符、休符、ポルタメントは同じ長さ表現を使う。
- ポルタメントの総長は255クロック以下。

## 6. 目標アーキテクチャ

### 6.1 物理タイマーB

`audio/opna/OpnaTimerB.kt` を追加し、`OpnaChipState` が一個だけ所有する。

必要な状態は次に限定する。

- 8ビットプリセット値。
- 現周期の整数余り。
- 実行中か。
- オーバーフロー通知が許可されているか。
- 現在の描画区間用に事前確保した通知列。

必要な操作は、アドレスやビットではなく次の意味名で表す。

- プリセット設定。
- 読み込み開始。
- 停止。
- 通知許可／禁止。
- 描画フレーム数だけ進め、オーバーフロー通知列を準備。
- リセット。

オーバーフロー通知は、ハードウェアのフラグ設定、割り込み通知、PMD処理、フラグ消去を一つの消費可能な境界通知として表す。将来用の汎用フラグ読み出しやレジスター状態は作らない。

周期進行には浮動小数点を使わず、次の整数比を余り付きで持ち越す。

```text
周期のマスタークロック数 = 1152 × (256 - NB)
オーバーフロー判定閾値 = サンプル周波数 × 周期のマスタークロック数
各サンプルで累積器へ加える量 = マスタークロック周波数
累積器が閾値以上ならオーバーフローを通知し、閾値を減算する
```

48kHz、8MHz、8ビットタイマーBでは一サンプル中の複数オーバーフローは起きないが、内部計算は範囲を名前付き法則で守る。

### 6.2 二層間の接続

呼び出し方向は現在どおり再生器から物理音源へ向ける。物理音源がPMD再生器を呼び返してはならない。

```text
CompiledOpnaPlayer
  1. 同時刻の大域タイマーB制御を処理
  2. OpnaLikeSynthesizerへ区間分のタイマーB通知列を準備させる
  3. 通知列をPmdPerformanceStateへ渡す
  4. PMD論理フレームから物理FM/SSG操作列を解決
  5. OpnaLikeSynthesizerへ音声区間を描画させる
```

これは物理音源がPMDイベントを解釈する構造ではない。タイマーBは物理周期だけを生成し、PMD層が通知の意味を所有する。

### 6.3 コンパイル時と実行時の一致

音符境界は引き続き事前計算し、音声コールバックでイベント整列やオブジェクト生成を行わない。

- コンパイル済み曲は、初期タイマーB値と、曲中の解決済みタイマーB変更列を持つ。
- 設定時は、その時点の `C` と音長形式から各イベントの整数PMDクロック位置を先に確定する。`C` は分母長をクロック数へ変換する法則であり、タイマーB周期そのものを変更する状態ではない。
- `PmdSampleClock` はBPM直接式ではなく、タイマーB周期法則と持ち越し余りからイベントのサンプル境界を作る。
- 実行時 `OpnaTimerB` と設定時 `PmdSampleClock` は、同じ周期法則、同じ変更境界規則、同じ余り規則を使う。
- 時系列にはタイマーB変更イベントを大域制御として残し、物理タイマー状態も同じ境界で更新する。
- ループ再開、巻き戻し、再生器交換では、イベントカーソル、PMD状態、タイマーBプリセット、周期余りを同じ初期状態へ戻す。

## 7. MML意味論の計画

### 7.1 型を分離する

現在の一種類の `MmlCommand.Tempo` を、少なくとも次の意味へ分ける。

- 音楽テンポ指定 `#Tempo` / `t`。
- タイマーB直接指定 `#Timer` / `T`。
- 既存TimeBox拡張の四分音符BPM指定 `#BPM`。

それぞれに絶対／相対の別を明示的に持たせる。符号を数値へ混ぜ、後段で偶然判定する形にはしない。

音長も一つの整数へ潰さず、設定時専用の名前付き表現へ分ける。

- 通常分母長: 値 `1..255`、内部長はその時点の `C ÷ 値`。
- 直接クロック長: `%` に続く値 `1..255`、内部長は値そのもの。
- 付点数: 真偽値ではなく0個以上の個数。

解析後の実行時時系列は、決定済み整数規則で解決したPMD内部クロック位置だけを基本型配列へ格納する。音声描画中に分母、`C`、付点を解釈し直さない。

全パート共有の相対変更は、トラック別コンパイル中に即時解決しない。全コマンドを時刻、既存の正規化済み原文順で並べた後、一つの大域タイミング状態に対して解決する。同時刻では後の原文順が最終値になる。

PMD版Bad Appleのように同じ `t80` が複数パートの同一時刻に現れる場合、同値の大域変更は一つへ正規化する。Gパート冒頭の `t70` → `t80` のように異なる値が長さ消費前の同一時刻に並ぶ場合は、原文順を維持して最終値 `t80` を採用し、長さゼロの架空テンポ区間を生成しない。

### 7.2 構文

- `#Timer 0..250` を初期直接指定として追加。
- `T0..255` と `T±値` を曲中直接指定として追加。
- `#Tempo 18..255` を初期PMD音楽テンポ指定として追加。
- 小文字 `t18..255` と `t±値` を曲中PMD音楽テンポ指定として追加。
- `#Zenlen 1..255` と大文字 `C1..255` を全音符内部クロック指定として追加。
- 通常の音符、休符、ポルタメント長 `n` は、その時点の `C` の約数であることを設定時に診断する。
- `%1..255` を直接PMDクロック長として追加し、通常分母長と型で区別する。
- 連続する複数の `.` を保持し、資料の `c2.. = c2&c4&c8` を表現できるようにする。
- `C` 指定時に既定音符長を4へ戻すPMD規則を、決定済みのパート作用域で反映する。
- ポルタメントは解決後の総長255クロック以下を診断する。W/S使用時だけの音符長制限を、未実装のW/Sと切り離して全音符へ課さない。
- 既存の `Q0..8` / `Q%0..255` は比例ゲート指定として維持する。
- `q` の生クロック形式に加えて `ql音長` 形式を追加する。生値は直接PMDクロック、`l音長` はその時点の `C` で通常分母長として解決する。
- `q` の短縮範囲、最低発音長、省略時の前値保持をPMDクロック単位で維持する。

現行のリズム用小文字 `t` はトム音を表すため構文衝突がある。製品曲の既存動作を勝手に変えないため、最初の実装では次のどちらかを明示的に選ぶ。

1. Rパートでは小文字 `t` をトムのまま維持し、テンポ変更は別パートまたはヘッダーへ置く。
2. PMD準拠を優先してRパートの曖昧さを解消する新しいリズム表記へ移行する。

既定案は1である。音響エンジン外へ影響させず、現在のリズム記法を保全できる。

### 7.3 既存構文の移行

- `#BPM` は現行製品曲の互換入力として残す。
- `#BPM` をタイマーBへ量子化するか、正確BPM拡張として別時間源を残すかは、実装前に明示的に決める。
- PMD版Bad Appleの原意は `C96` と `t80` である。`#BPM 160.73` は互換入力であって、正本の意味論ではない。
- タイマーBを唯一のPMD時間源にする目標からは、`t80` を決定済みのPMD変換則でタイマーB値へ解決する案を推奨する。ただし全イベント境界と総時間がわずかに変わり得るため、無断で採用しない。
- `#PMDCLOCK 24` は現行互換として残し、`#Zenlen 96` と同じ意味へ正規化できる。4倍した値が許容範囲を外れる場合、またはPMD規則と矛盾する場合は診断する。
- PMD版Bad Apple全体の取り込みは、未対応音色・リズム・演奏構文まで含む別作業である。今回の計画では `t80`、同時刻の大域指定、`MXA1 MXB1`、59個の `%` 直接クロック長を実曲の時間根拠として使う。
- LOGO曲には互換性を約束しない。

## 8. タイマーBで進めるPMD状態

### 8.1 通常速度ソフトウェアLFO

- `PmdSoftwareLfo` の通常モードからBPM由来の独立サンプル位相を除く。
- 通常モードはタイマーB由来のPMD内部クロック通知が来たときだけ一段進める。
- 遅延、速度、波形、反復、深度変化の既存順序は変えない。
- 固定モードは約56Hzの整数余り付きサンプル位相を維持する。
- キーオン同期、自由走行、二つのLFO、FM3論理パート所有は維持する。

### 8.2 通常速度ソフトウェア包絡

- `PmdSoftwareEnvelope` の通常モードも同じPMD内部クロック通知で進める。
- 拡張モードは約56Hzを維持する。
- アタック、減衰、持続、リリースのアルゴリズム本体と初期値は、タイマー変更とは別の仮説なので変更しない。
- SSGリリース完了フレームの検出順序を維持する。

### 8.3 PMDクロック指定の内蔵LFO遅延

現在の生PMDクロック遅延は、キーオン時にBPMから固定フレーム数へ変換される。この方式では遅延途中の `T` 変更へ追従できない。

- 生クロック遅延はPMD層でタイマーB通知ごとに減算する。
- 音符長形式の遅延も、キーオン時点の全音符クロック規則から必要クロック数へ解決する。
- `PmdFmFrame` / `FmRenderBinding` に、物理LFOをそのフレームで適用するかを表す事前確保列を渡す。
- コンパイル済み経路では `Fm4OpVoice` のサンプル単位遅延を使わない。
- 直接 `OpnaSequencer` 経路の既存フレーム遅延は、この計画では維持する。

### 8.4 ゲートとキーオフ境界

- 音符の総長をPMD内部クロックで確定してから、`Q` / `Q%` の比例位置を整数で求める。
- `q` の直接クロック、または `ql音長` から解決したクロックを比例位置から引く。
- 結果が最低発音長より短ければ最低値へ、音符総長より長ければ総長へ制限する。最低値0は資料どおり1クロックとして扱う。
- `C96 Q4 q2 c4` は、総長24、比例位置12、短縮2、解決済みゲート10クロックになる。
- 解決済みキーオフのPMDクロック位置を、音符開始・終了と同じタイマーB法則でサンプル境界へ変換する。ゲートだけを固定480刻みやBPM直接式へ残さない。
- 既存の決定的な `q` 乱数状態とスラー時のキーオフ抑制は維持し、タイマー統合と同時に乱数系列やスラー意味を変更しない。

### 8.5 タイマーBへ接続しない状態

- YM2608内蔵LFOの発振位相。
- FMハードウェア包絡。
- SSGハードウェア包絡。
- FMオペレーター位相。
- ポルタメント。
- ミキサー、EQ、フィルター、クリップ。

特にポルタメントは、サンプル補間からクロック段階へ変えると時間軌跡が変化する。選択資料はその規則を示していないため、今回の計画へ含めない。

## 9. 段階的な実装順序

各段階は一つの仮説だけを扱い、前段の差分と論理が確認されるまで次へ進まない。

### 段階0: 不足仕様を確定

成果:

- タイマーBオーバーフロー2回対PMD内部1クロック候補の確認。
- `t` / `#Tempo` の整数変換と丸め。
- 実行中プリセット変更時の再読み込み規則。
- 付点端数の整数処理。
- `#BPM` の互換方針。
- Rパート小文字 `t` の衝突方針。

この段階が未確定のまま、変換定数や時系列計算を実装しない。

### 段階1: 物理タイマーBを追加

変更候補:

- 新規 `shared-engine/src/commonMain/.../audio/opna/OpnaTimerB.kt`
- `audio/opna/OpnaChipState.kt`
- `audio/opna/OpnaLikeSynthesizer.kt`

内容:

- 8MHz周期式の整数実装。
- 事前確保された区間通知列。
- プリセット、読み込み開始、停止、通知許可、リセット。
- 自動再読み込みと余り持ち越し。
- タイマーA、バス待機、レジスター窓口が存在しないことを差分で確認。

### 段階2: MMLタイミング意味を型として分離

変更候補:

- `audio/mml/MmlParser.kt`
- `audio/mml/MmlCompiler.kt`
- `audio/mml/CompiledOpnaSong.kt`
- `audio/mml/PmdPerformanceLaws.kt`

内容:
User note T and t are 2 differnt things as well as C and c.  also C and %値  are differnt things as well.  most of the logic in timerplan.md is wrong please consult with the following alwas. "D:\Programes\ym2608-info\timeboxinfo\timerinfo\gemini exeplnation of timer logic refined.txt"
"D:\Programes\ym2608-info\timeboxinfo\timerinfo\gemini on overflow.txt"
"D:\Programes\ym2608-info\timeboxinfo\timerinfo\gemini exeplantion of timer logic 1.txt"

- `#Timer` / `T` と `#Tempo` / `t` を別の型にする。
- 絶対／相対を別フィールドにする。
- 全パート共有値を正規化済み原文順で解決する。
- 初期値と曲中変更を解決済みタイマーB列として曲プログラムへ格納する。
- `#Zenlen` / `C` と合法な音符長を追加する。
- 通常分母長、`%` 直接クロック長、複数付点を別の設定時表現にする。
- 音符、休符、ポルタメントを同じ長さ解決器へ通し、ポルタメントだけに確定済み255クロック上限を適用する。
- `q` の生クロック形式と `ql音長` 形式を型で区別し、`Q` 比例位置、後端短縮、最低発音長を整数PMDクロックへ解決する。
- `#BPM` / `#PMDCLOCK` の決定済み互換方針を反映する。

### 段階3: サンプル境界をタイマーB時間軸へ移す   **check and see if this is even needed or valid**

変更候補:

- `audio/mml/PmdSampleClock.kt`
- `audio/mml/CompiledOpnaTimeline.kt`
- `audio/mml/CompiledOpnaSong.kt`

内容:
User note T and t are 2 differnt things as well as C and c.  also C and %値  are differnt things as well.

- 通常長を `C ÷ 分母`、直接長を `%値` として整数PMDクロックへ解決し、累積PMDクロックを時系列の正準座標にする。
- 現行の固定480刻みは `#BPM` / `#PMDCLOCK` 互換入力の変換境界に限定し、PMD正本の時間源として残さない。 **the logic here is wrong also we do not need or want backwards copatability with #BPM,  it should be removed imo**  
- 各PMDクロック境界までの旧タイマーB周期を整数余り付きで積算する。
- タイマーB変更境界を時系列の大域制御として出力する。
- 境界直前は旧値、境界後は新値という決定済み規則を一箇所で実施する。
- C96と `#PMDCLOCK 24` の既存曲では、通常分母長が現行の固定480刻みと同じ音楽位置へ解決されることを保つ。 **the logic here is wrong** 
- `%` 長は指定PMDクロック数をそのまま進める。通常分母へ逆変換して精度を失わせない。
- 解決済み `Q` / `q` キーオフ位置も同じ累積PMDクロック座標へ置き、音符境界と同じタイマーB変換を使う。

### 段階4: 実行時タイマーB通知をPMD状態へ接続

変更候補:

- `audio/mml/CompiledOpnaPlayer.kt`
- `audio/mml/PmdPerformanceState.kt`
- `audio/mml/PmdSoftwareLfo.kt`
- `audio/mml/PmdSoftwareEnvelope.kt`
- `audio/opna/OpnaLikeSynthesizer.kt`

内容:

- 再生器が各描画区間のタイマーB通知列を物理層から受け取る。
- 通常LFOと通常包絡を同じPMD内部クロック通知で進める。
- 固定56Hzモードを分離したまま維持する。
- リセット、巻き戻し、ループ境界でタイマー位相とPMD状態を同時に初期化する。
- 音声コールバックで割り当て、コレクション操作、除算の乱用、三角関数を追加しない。

### 段階5: PMDクロック指定の内蔵LFO遅延を統合

変更候補:

- `audio/mml/PmdPerformanceState.kt`
- `audio/mml/CompiledOpnaPlayer.kt`
- `audio/opna/OpnaDriverFrames.kt`
- `audio/opna/Fm4OpVoice.kt`

内容:

- 遅延カウンターをPMD層でタイマーB通知により進める。
- 解決済み適用許可列だけを物理FMへ渡す。
- 物理内蔵LFOの周波数、位相、PMS、AMS、オペレーターAM許可は変更しない。
- 直接シーケンサー経路へ影響させない。

### 段階6: 資料にある大域速度指定を完成

変更候補:

- `audio/mml/MmlParser.kt`
- `audio/mml/MmlCompiler.kt`
- `audio/mml/CompiledOpnaSong.kt`

内容:

- `#LFOSpeed Normal/Extend` を追加し、FM/SSGの両LFOへ初期クロックモードを与える。
- 既存のパート別 `MX` は大域初期値を上書きできる。
- 既存 `#EnvelopeSpeed Normal/Extend` は、通常側だけをタイマーB通知へ接続した意味になる。
- 存在しないADPCMパートを追加しない。

### 段階7: 任意の隣接改善—SSG拡張デチューン

これはタイマーB統合の完了条件ではない。別の音響仮説として扱う。

変更候補:

- `audio/mml/MmlParser.kt`
- `audio/mml/MmlCompiler.kt`
- `audio/mml/PmdPerformanceState.kt`
- 必要なSSG音程変換ファイル

内容:

- `#Detune Normal/Extend` とパート別 `DX` の有無を設計する。
- 通常モードは現在の音程値加算を維持する。
- 拡張モードは音域依存補正をPMD層で解決してから物理SSG周期へ渡す。
- タイマー、内蔵LFO、ミキサーとは混ぜない。

## 10. 実装時の変更許可一覧

計画実施前に、その回で扱う段階だけを選び、次の候補から実際の変更ファイルをさらに狭める。

```text
audio/opna/OpnaTimerB.kt                 新規候補
audio/opna/OpnaChipState.kt
audio/opna/OpnaLikeSynthesizer.kt
audio/opna/OpnaDriverFrames.kt
audio/opna/Fm4OpVoice.kt
audio/mml/MmlParser.kt
audio/mml/MmlCompiler.kt
audio/mml/CompiledOpnaSong.kt
audio/mml/CompiledOpnaTimeline.kt
audio/mml/CompiledOpnaPlayer.kt
audio/mml/PmdSampleClock.kt
audio/mml/PmdPerformanceLaws.kt
audio/mml/PmdPerformanceState.kt
audio/mml/PmdSoftwareLfo.kt
audio/mml/PmdSoftwareEnvelope.kt
```

次のファイルは呼び出し元として確認済みだが、通常は変更しない。

```text
audio/mml/MmlArrangementScheduler.kt
audio/mml/MmlSongBank.kt
engine/SongCatalog.kt
app/.../SoundPreviewPlayer.kt
audio/opna/Lfo.kt
audio/opna/OpnaLfoLaws.kt
```

実装中に一覧外の本番ファイルが必要になった場合は、その理由と最小変更を提示して許可を得る。

## 11. 完了判定

### 論理上の完了条件

- タイマーB周期が8MHzの資料式から整数で導かれる。
- タイマーA、バス待機、レジスター窓口が存在しない。
- `#Timer` / `T` と `#Tempo` / `t` の意味が混同されない。
- PMD版Bad Appleの `t80` をBPM80やタイマーB直接値80として誤解釈しない。
- 通常音長 `n` がその時点の `C ÷ n`、`%n` が直接 `n` PMDクロックとして解決される。
- 音符、休符、ポルタメントが同じ音長・複数付点法則を使い、ポルタメントの255クロック上限だけが固有に守られる。
- `Q` / `Q%`、`q` 生クロック、`ql音長`、最低発音長がPMDクロックで解決され、キーオフだけが別時間源へ残らない。
- 現行の `#PMDCLOCK 24` / 固定480刻み互換が `C96` のPMDクロック位置と一致する。
- 全パート共有の相対タイミング変更が正規化済み原文順で解決される。
- 音符時系列、通常LFO、通常包絡、生クロック遅延が同じPMD内部クロック通知を使う。
- 拡張LFO・包絡は約56Hzの独立時間源を維持する。
- YM2608内蔵LFOとFMハードウェア包絡はタイマーBから独立したままである。
- 実行時描画は事前確保状態と基本型反復だけを使う。
- ループと巻き戻しでタイマーB位相を含む全所有状態が再現可能に初期化される。

### 構造上の確認

- 対象は `commonMain` の音響コードであり、プラットフォーム相互運用には触れない。
- 音声ホットパスへ新しい割り当て、ラムダ、コレクション反復を追加しない。
- 外部ファイル、音声素材、設定素材を追加しない。
- `OpnaLikeSynthesizer` がPMDイベント解釈やPMDパート状態を所有しない。
- `CompiledOpnaPlayer` から物理層への呼び出し方向を維持し、物理層から再生器へのコールバックを作らない。

### 音響上の確認

タイミング変更は音響変更である。コンパイル成功や数式一致だけでは採用完了としない。

- Bad Appleの総時間、音符境界、ゲート、SSG包絡、FM/SSG相対位置を確認する。
- PMD版の `C96` / `t80` と現行製品版の時間軸を比較し、量子化による差を明示する。
- 同一時刻に重なる各パートの `t80` と、Gパートの `t70` → `t80` が余分な時間区間を作らないことを確認する。
- Bad Appleで59回使われる `%` 長が、通常分母へ丸め直されず指定PMDクロック位置を保つことを確認する。
- `C96 Q4 q2 c4` 相当でキーオフが音符開始から10PMDクロック目に置かれ、タイマーB由来の正しいサンプル境界へ変換されることを確認する。
- 通常LFO、拡張LFO、通常包絡、拡張包絡を別々に聞き分ける。
- タイマーB変更をまたぐLFO遅延とリリース尾を確認する。
- 人間の同音量聴取が終わるまでは、「構造確認済み、音楽的採用未完了」と記録する。

ユーザーが別途明示的に許可しない限り、検査コードの作成・実行は計画へ含めない。本番に検査専用状態や記録機構を残さない。

## 12. 推奨する最初の実装単位

最初の実装は、段階0の不足仕様を確定した後、段階1の物理タイマーBだけに限定する。

この単位ではMML構文、曲時間、LFO、包絡をまだ変えない。8MHz周期、直接的な開始・停止・通知論理、無割り当て区間通知だけを完成させ、差分がタイマーB物理則だけを表していることを確認する。その後、MML時間軸とPMD変調を別々の変更単位で接続する。


## **THE folowing MUST ALWAYS BE CONSOULTED WITH WHILE IMPLIMENTING ANY PART OF THIS PLAN** The overflow logic was misunderstood when this plan was written.
***user notes*** this overrules all previous logic and math as it has be verified by a human.
overflow is a hardware concept YM2608 only. and PMD is not directly related or has  overflow.
This is  hardware logic not  software logic.
0. オーバーフローと音楽テンポ（BPM）の決定的な関係
このハードウェアのオーバーフローという「物理的現象」は、ソフトウェア（PMD）に対して以下のような手順で「音楽的な時間」をもたらします。
1. タイマーフラグと割り込み（IRQ）の発生： Timer Bがオーバーフローすると、YM2608の内部ステータスレジスタの「タイマーBフラグ（D1）」に自動的に 1 が立ちます これが引き金となり、YM2608の物理ピンである IRQ 端子が Low レベルに変化し、CPUに対して割り込み信号を発生させます

2. PMDによる「1Tick」のカウント： 割り込みを検知したPC-9801のCPUは、PMDの「割り込みハンドラ」を起動します。PMDはこのオーバーフロー信号が届くたびに、音楽データ（MML）の時間を 1Tick（1クロック）進めます。

*overflow まとめ*
- 物理的な「オーバーフロー」（Hardware）： YM2608のTimer Bが 255 から 0 にあふれる瞬間のこと この「あふれ」がCPUへの割り込み信号（IRQ）となり、PMDの演奏時計を 1Tick ずつ進めます

- 音楽的な「テンポ」への影響（Math）： 256 から設定値を引いた「オーバーフローまでの時間（あふれ周期）」が、MMLの音符の長さ（クロック数）と掛け合わされることで、最終的なBPM（160.75など）が決定されます

- データ的な「オーバーフロー」（Software）： PMD内部の変数の限界値（16ビットや8ビット）を超えてデータがあふれることで、LFOによる音の裏返りなどのバグや、特殊な数値回り込みが発生します

- このように、オーバーフローはFM音源ドライバにおける**「時間創出のトリガー」であり、同時に「システム限界の境界線」**でもある、極めて重要な役割を持った言葉なのです。

## If this correction on timer info is not enough please consult the following two files for a more indeapth exeplnation of timer logic "D:\Programes\ym2608-info\timeboxinfo\timerinfo\gemini exeplantion of timer logic 1.txt" "D:\Programes\ym2608-info\timeboxinfo\timerinfo\gemini exeplnation of timer logic refined.txt