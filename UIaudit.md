# UI・グラフィックスシステム監査

監査日: 2026-07-30

## 1. 対象と立場

対象は次のディレクトリ内に限定した。

`shared-engine/src/commonMain/kotlin/com/example/timeboxvibe/engine/core`

本書は静的なコード監査であり、設計思想への適合・不適合を一方的に決めるものではない。観察できる実装、現在起こり得る影響、改善する場合の選択肢を分離して記述する。

今回、コード、テスト、ビルド設定、実行時資産には変更を加えていない。ビルド、テスト、実機操作、画面比較も実施していない。

### Uに関する前提

- UIの正しい標準単位は常に `U = 16` である。
- 各UI・描画モジュールが、それぞれローカルに `U = 16` を定義する現在の形は、意図された正しい設計である。
- したがって、本書は複数箇所の `U = 16` 定義を重複や中央集約不足として扱わない。
- 監査対象は、UI寸法で `U` を使わず、生のピクセル値、固定解像度、または説明のない別基準を使う箇所である。
- 手続き的アイコンの32×32内部座標、角度、円周分割数などは、UI配置寸法とは別の局所的な図形表現になり得るため、直ちに問題とは判定しない。

## 2. 全体所見

指定範囲には、次の良好な一貫性がある。

- UIフレームワーク、Android型、Java API、外部画像資産への依存は確認されなかった。
- 通常のUI色は `PaletteIndices` の0～15を使っている。
- `AliasedVectorLayer` の1画素線は入口で整数化し、基本の走査は明示的な `while` ループで行われる。
- 回転字形バッファは事前確保され、描画ごとの配列生成を避けている。
- `TemplateForgeScene` と `SettingsScene` には、文字幅を測って横並びと縦積みを選ぶ、内容依存の配置処理がある。
- HUDの描画式と当たり判定式は、現時点では同じ数式を使っている。

一方、優先して確認する価値が高い所見は次のとおりである。

| ID | 重要度 | 到達性 | 概要 |
|---|---|---|---|
| B-01 | 高 | 現行 | 警報表示の背後にある不可視HUDを操作できる |
| B-02 | 高 | 現行 | エントロピー画面の描画位置と当たり判定位置がずれる |
| B-03 | 高 | 現行 | テキスト編集カーソルと実際の挿入位置が一致しない |
| B-04 | 高 | 現行 | 魔法陣の10文字リングで2文字が既存位置へ重なる |
| B-05 | 高 | 現行 | 演出の主スイッチを切っても彗星尾が描画される |
| B-06 | 高 | 現行 | アイコンの未対応色が主色へ潰れる |
| B-07 | 高 | 現行条件付き | 拡大字形のクリップが矩形全体を制限しない |
| B-08 | 中～高 | 現行 | 太線がCanvas外・小数座標の矩形を出し得る |
| B-09 | 中～高 | 現行 | 狭い、または極端に低い表示でUIが収まらない |

## 3. 現行経路で確認できる不具合・不整合

### B-01 警報表示の背後にあるHUDを操作できる

重要度: 高

根拠:

- `Scenes.kt:312-318`
- `Scenes.kt:482-510`

観察:

通常HUDを描いた後、警報中は全画面を警報表示で塗りつぶす。一方、入力では警報状態の判定より先に `RetroHudComponent.onTouchEvent` が処理される。

影響:

- 画面上では見えないHUDタブをタップして別シーンへ遷移できる。
- 表示上は `BOMB / DISMISS` ボタンがあるが、HUD以外では任意の `UP` が警報解除になる。表示される操作契約と実際の入力契約が一致していない。

改善する場合の選択肢:

- 警報を最上位のモーダル入力として先に処理する。
- ボタン矩形だけで解除する。
- 「画面のどこでも解除」が意図なら、表示文言をその仕様に合わせる。

### B-02 エントロピー画面の描画位置と当たり判定位置がずれる

重要度: 高

根拠:

- 描画: `Scenes.kt:2687-2704`
- 入力: `Scenes.kt:2888-2909`

観察:

描画側の `safeTop` は `playAreaH / 12f`、入力側は `cachedLogicalHeight / 12f` から求める。下部HUDを使う配置では、プレイ領域高と全体高は同じではない。

影響:

入力欄と追加ボタンの表示位置に対し、タップ領域が縦方向へずれる。

改善する場合の選択肢:

描画と入力の双方で、同じ `playAreaH` と同じレイアウト計算を使う。

### B-03 テキスト編集カーソルと実際の挿入位置が一致しない

重要度: 高

根拠:

- `FixedInputContainer.kt:8-9`
- `FixedInputContainer.kt:40-41`
- `Scenes.kt:266-298`

観察:

`FixedInputContainer.cursor` は左右入力で移動するが、タスク欄の描画カーソルは表示文字列の末尾から計算される。

影響:

- 左右キーでカーソルを動かした後、見えるカーソルと挿入・削除位置が異なる。
- 空欄フォーカス時は文字列の `_` と `EngineCursorRenderer` の両方が描かれ、実際の位置より右へ見える。
- 長文時も、編集カーソルを必ず含む表示窓にはなっていない。

改善する場合の選択肢:

描画位置を `inputContainer.cursor` から求め、長文時はカーソルを含むセル範囲を表示する。カーソル表現は一種類に揃える。

### B-04 魔法陣の10文字リングで2文字が重なる

重要度: 高

根拠:

- `NestedTimeboxInstrumentRenderer.kt:220-235`

観察:

10文字に対して、角度間隔は `(360 / 10) * 1.25 = 45度` になっている。8番目は0番目と同じ360度位置、9番目は1番目と同じ405度位置へ戻る。

影響:

10文字のうち2文字が既存位置へ重なり、実際には8つの固有位置になる。

改善する場合の選択肢:

- 10文字を均等配置するなら1.25倍を外す。
- 45度刻みが意図なら、文字数と名称を8位置の契約に合わせる。

### B-05 演出の主スイッチを切っても彗星尾が描画される

重要度: 高

根拠:

- `VisualsStateHolder.kt:20-27`
- `NestedTimeboxInstrumentRenderer.kt:35-36`
- `NestedTimeboxInstrumentRenderer.kt:171`
- `NestedTimeboxInstrumentRenderer.kt:295-316`

観察:

設定は全デモシーン演出の主スイッチとして説明されている。しかし現在、`demosceneEffectsEnabled` が実際に止める現行描画効果は、主に `runeDriftAngleOffset` のPerlin揺らぎである。4点の彗星尾は設定に関係なく描画される。

影響:

利用者が演出を無効化しても、説明上は無効になるはずの尾が残る。

改善する場合の選択肢:

- 尾の描画も同じ設定で制御する。
- 尾を常設意匠とするなら、設定名と説明をその範囲に合わせる。

### B-06 アイコンの未対応色が主色へ潰れる

重要度: 高

根拠:

- `ProceduralUiPrimitives.kt:5-12`
- `ProceduralUiPrimitives.kt:28-56`
- `ProceduralMath.kt:13-240`

観察:

アイコン生成値にはパレット番号とARGB値が混在する。次のARGB値は `mapIconColor` に対応分岐がなく、`else -> primaryColor` へ落ちる。

- `0xFFFFAAA6`
- `0xFFD4AF37`
- `0xFF333333`
- `0xFFCCCCCC`

影響:

- `ofuda` の淡色ディザ
- `hakkero` の金色と暗色
- `watch` の銀色と金色

これらが主色へ潰れ、生成関数内の意図した色差が出ない。

改善する場合の選択肢:

- 生成段階からパレット番号を返す。
- 小さな「アイコン色役割」番号を返し、一つの変換関数で全役割をパレットへ対応させる。

補足:

アイコン側は0を透明番兵として扱うため、`primaryColor = PaletteIndices.BG` を渡すと、有効なBG色も透明扱いになる。現在の呼び出しでBGを主色にしていなければ未顕在である。

### B-07 拡大字形のクリップが画素矩形全体を制限しない

重要度: 高

根拠:

- `ScaledProceduralRenderer.kt:114-128`

観察:

クリップ判定は各拡大画素矩形の左上 `drawX/drawY` だけを見る。実際には `scale × scale` の矩形を書き込む。

影響:

- `scale > 1` で右端または下端に接する字形が、UIクリップ領域からはみ出す。
- 左端・上端では左上が外なら矩形全体を省略するため、左右・上下で部分表示の扱いが非対称になる。
- プラットフォームCanvasが画面端を保護しても、UI独自のクリップ枠からのはみ出しは残る。

改善する場合の選択肢:

拡大画素を整数画素単位でクリップするか、画素矩形とクリップ矩形の交差部分だけを出力する。

### B-08 太線の境界判定と整数化が1画素線と一致しない

重要度: 中～高

根拠:

- `AliasedVectorLayer.kt:452-459`
- 現行の太線呼び出し例: `Scenes.kt:359-360`

観察:

中心点だけをCanvas内判定した後、`strokeWidth > 1` では中心の周囲へ `drawRect` を出す。奇数幅では `strokeWidth / 2f` により矩形原点が0.5座標になる。

影響:

- 画面端で矩形の一部がCanvas外へ出る。
- 最終座標が整数でなくなる。
- 端処理と丸め結果がプラットフォーム実装に依存し得る。

改善する場合の選択肢:

太線の開始・終了を整数で求め、Canvas範囲へ切り詰めた後、整数画素または整数矩形として出力する。

### B-09 極端な表示比率でHUDが収まらない

重要度: 中～高

根拠:

- `RetroHudComponent.kt:37-39`
- `RetroHudComponent.kt:100-120`

観察:

左HUDを使うかどうかは論理幅だけで決まる。左HUD採用後は、縦方向が不足してもボタン高を最低 `U * 3 / 2` に保つ。

影響:

幅広で非常に低い表示では、4ボタンと間隔の合計が画面高を超える。描画と当たり判定は同じ式なので互いには一致するが、両方が表示範囲外へ出る。

改善する場合の選択肢:

左HUDに必要な幅と高さを両方計算してから、左配置または下配置を選ぶ。

### B-10 タイマー円の最小半径が利用可能領域より大きくなり得る

重要度: 中～高

根拠:

- `Scenes.kt:594-598`

観察:

利用可能幅・高さから得た半径上限より、`U * 8` の最小半径を `maxOf` で優先する。

影響:

プレイ領域が直径 `U * 16` 未満の場合、円は必ず横または縦にはみ出す。縦スクロールでは横方向の欠けを解消できない。

改善する場合の選択肢:

必要寸法が収まるかを先に判定し、収まらないときだけ小領域用配置へ切り替える。

### B-11 設定画面の視覚効果見出しが二重描画される

重要度: 低～中

根拠:

- `Scenes.kt:2414-2440`
- `Scenes.kt:2483-2487`

観察:

`layoutRow(renderer, strings.visualsHeader)` がラベルを描いた直後、`drawTextRaw` でも同じ文字列を描く。

影響:

横並び時は同じ見出しの重ね描き、縦積み時は別位置への二重表示になる。

### B-12 エントロピー画面の削除後ページ補正がページ容量を使わない

重要度: 中

根拠:

- 通常計算: `Scenes.kt:2722-2724`
- 入力計算: `Scenes.kt:2922-2925`
- 削除後計算: `Scenes.kt:3044-3045`

観察:

通常は `rowsPerPage` で総ページ数を求めるが、削除後だけ `MAX_TASKS` を分母にする。

影響:

複数ページ時に1件削除すると、現在ページがまだ有効でも0ページへ戻されやすい。

### B-13 32ビットコードポイントの扱いが画面ごとに異なる

重要度: 中

根拠:

- `FixedInputContainer.kt:3-5`
- `Scenes.kt:410-423`
- `Scenes.kt:2072-2082`
- `Scenes.kt:3090-3100`
- `Scenes.kt:3125-3136`

観察:

入力コンテナは32ビットコードポイントを保存する。タイマー画面はサロゲート対へ変換するが、テンプレート編集では `0xFFFF` 超を捨て、エントロピー表示では `?` にし、別の文字列化経路では捨てる。

影響:

同じ入力が画面により保存、表示、転送時に変化または消失する。

改善する場合の選択肢:

コードポイントから表示文字列または代替字形へ変換する規則を、一つの手続き的入力部品として共有する。

### B-14 中央文字列の幅制限が一部にしかない

重要度: 中

根拠:

- `NestedTimeboxInstrumentRenderer.kt:434`
- `NestedTimeboxInstrumentRenderer.kt:472-534`

観察:

段階名には最大幅があるが、静的ラベル、時刻、警報時刻には同じ幅制限がない。また、最大幅が負でも段階名は最低1文字を描く。

影響:

狭い表示、長い翻訳、長い分数表示で、中央の静穏領域またはプレイ領域から文字がはみ出し得る。

## 4. 条件付き、未使用経路、または防御性の所見

### C-01 1引数版の更新はフレーム時計を二度進める

重要度: 中

根拠:

- `SceneManager.kt:94-108`

1引数版 `update(dt)` は `FrameClock.tick()` 後に3引数版を呼び、3引数版も `tick()` する。監査範囲内には1引数版の呼び出し元がないため、現在の製品経路での到達性は未確認である。

### C-02 現行の視覚時間は60更新毎秒を仮定する

重要度: 中

根拠:

- `FrameClock.kt:25-50`
- `Scenes.kt:149`

`FrameClock` は `dt` ではなく更新回数を数え、現行の魔法陣は `FrameClock.seconds(60f)` を使う。更新頻度が60Hzでなければ、回転速度は実時間と一致しない。固定60Hzが明示された契約なら現状は成立するが、その契約はこの境界からは保証されない。

### C-03 `Wave` の位相単位と折り返し単位が一致しない

重要度: 高、ただし現在の出力では未使用

根拠:

- `Wave.kt:24-27`
- `Wave.kt:34-55`

`value()` は `sin(phase * 2π)` として `phase` を周期単位で扱う一方、`update()` は `phase > 2π` で `2π` を引く。約6.283周期ごとに位相が不連続になる。

現在、6個のWaveは更新・リセットされるが、対象範囲内で `value()` または `valueNorm()` の描画利用は確認されない。そのため、現行画面の見た目を壊す不具合ではなく、再接続時に顕在化する問題である。

### C-04 FABRIK実装は無割り当てではなく、現在は描画へ接続されていない

重要度: 高、ただし未使用経路

根拠:

- `IkChain2D.kt:19`
- `IkChain2D.kt:35-78`
- `IkChain2D.kt:85-106`
- `MagicCircleDemoscene.kt:95-98`

`solve()` はループ中に多数の `Point2D` を生成する。また、毎回直線へ初期化するため、説明にある履歴的な「遅れ」は保持されない。

対象範囲内で `solveTrail()` の呼び出しと `trail.points` の描画利用は確認されない。現在の彗星尾は `NestedTimeboxInstrumentRenderer` が別の4点描画として所有する。

### C-05 未使用の進捗目盛APIは0%でも1本描く

重要度: 低～中、未使用経路

根拠:

- `ScaledProceduralRenderer.kt:317-365`

`activeOuter = (progress * 60).toInt()` に対して `i <= activeOuter` を使うため、0%でも1本、50%で31本になる。対象範囲内ではこの関数の呼び出しは確認されない。

### C-06 シーン切替失敗時に退避先の入場処理を呼ばない

重要度: 中

根拠:

- `SceneManager.kt:71-91`

`onExit` または新シーンの `onEnter` が例外を投げると、`activeScene = SettingsScene` にするだけで `SettingsScene.onEnter` は呼ばれない。例外時だけの経路だが、シーンの一時状態が初期化されない可能性がある。

### C-07 タッチバッファ例外ログが再び同じ範囲外位置を読む

重要度: 中、入力破損時のみ

根拠:

- `SceneManager.kt:247-270`

バッファ長と `touchCount` の整合性検査がなく、範囲外例外を捕捉した後のログでも `touchBuffer[offset + TOUCH_SLOT_ACTION]` を読む。最初の例外を捕捉しても、ログ生成中の二度目の例外が更新処理へ漏れ得る。

### C-08 パレット公開状態は更新番号を迂回できる

重要度: 低～中、API防御性

根拠:

- `Pc98GraphicsHardware.kt:34-54`

`onScreenPalette` の配列内容と `paletteRevision` が公開可変である。外部が配列要素を直接変更すると、更新番号が増えない。現在の対象範囲内では、`setupPalette` 外からの直接書き換えは確認されない。

## 5. Uを使っていないUI寸法と固定基準

### H-01 アクティブタイマー画面

根拠:

- `Scenes.kt:45-54`
- `Scenes.kt:241-249`
- `Scenes.kt:301-310`
- `Scenes.kt:514-590`

主な値:

- `TASK_INPUT_SIDE_PAD = 20f`
- `TASK_INPUT_HEIGHT = 36f`
- `TASK_INPUT_INNER_PAD = 8f`
- `PRESET_BADGE_SIZE = 36f`
- `PRESET_BADGE_GAP = 8f`
- `CONTROL_BUTTON_HEIGHT = 42f`
- `CONTROL_GAP_PORTRAIT = 10f`
- `CONTROL_GAP_LANDSCAPE = 16f`
- `CONTROL_ICON_SIZE = 32f`
- プリセットバッジ表示条件 `playAreaW >= 220f`

`8f`、`16f`、`32f` は現在のUと一致するため、それぞれ `U / 2`、`U`、`U * 2` で意図を表せる。20、36、42、10、220はU刻みと一致しないため、残すなら内容要求、操作最小寸法、または部品固有寸法として理由を明示する余地がある。

### H-02 テンプレート一覧

根拠:

- `Scenes.kt:960-1053`
- `Scenes.kt:1122-1200`

カード最低高60、間隔6、上余白30、左右余白20/40、削除ボタン60×26、端位置90、作成ボタン92×26などが、描画と入力の両方に直接記述されている。

値そのものが必ず誤りとは限らないが、U基準の他画面と寸法表現が異なり、描画・入力の片側だけが変わる余地がある。

### H-03 テンプレート編集のジェスチャー閾値

根拠:

- `Scenes.kt:1437-1451`

ドラッグ判定に2と8を直接使う。他の画面で同じ役割に `U / 4` と `U / 2` を使う箇所があり、画面ごとの操作感と表現が一致していない。

### H-04 ブロックオーバーレイ

根拠:

- `Scenes.kt:3159-3186`

最低幅200、最低高32、配置比率0.3、0.45、0.7などを描画と入力で重複して使う。小さい画面では最低幅200が論理幅を超え得る。

32は `U * 2` と表現できる。最低幅は文字幅とU由来の左右余白から必要幅を計算し、利用可能幅で上限拘束する選択肢がある。

### H-05 640×400の初期キャッシュ

根拠:

- `Scenes.kt:70-71`
- `Scenes.kt:917-918`
- `Scenes.kt:1232-1233`
- `Scenes.kt:2098-2099`
- `Scenes.kt:2606-2607`

複数シーンが論理寸法の初期値として640×400を持つ。通常は最初の描画で実寸へ更新されるが、テンプレート編集とエントロピー入力の一部はキャッシュを読む。

通常ライフサイクルで必ず描画が入力より先なら未顕在だが、初回描画前の入力では固定解像度が当たり判定へ影響し得る。

### H-06 HUD比率の逆算が各シーンに散在する

根拠:

- `Scenes.kt:460`
- `Scenes.kt:493`
- `Scenes.kt:1064`
- `Scenes.kt:1120`
- `Scenes.kt:1419`
- `Scenes.kt:1476`
- `Scenes.kt:2192`
- `Scenes.kt:2222`
- `Scenes.kt:3179`

複数シーンが `playH * 20f / 17f` で全論理高を復元し、HUDの17/20比率を直接知っている。整数化された `playH` から逆算するため、元の全体高を完全には復元できない場合もある。

これはUの問題ではなく、HUD配置の所有情報がシーンへ漏れている問題である。

### H-07 描画基盤内の生値

根拠:

- `ProceduralUiPrimitives.kt:4`
- `ScaledProceduralRenderer.kt:323-352`

`ICON_SIZE = 32`、線幅2、半径差4、内側差24、目盛長4/2は、現在のUに対して `U * 2`、`U / 8`、`U / 4`、`U + U / 2` と表せる。

ただし、32×32が手続き的アイコン固有の格子であり、60分目盛が計器固有の法則なら、UIレイアウト寸法へ無理に統合する必要はない。その場合は、Uとは別の局所基準であることを名前またはコメントで明確にするのが中立的な整理になる。

## 6. ホットパスと割り当て

### P-01 アクティブタイマー描画で一時オブジェクトを作る

根拠:

- `Scenes.kt:188-196`
- `SessionMacroDisplay.kt:47-64`

毎フレームの描画で次が行われる。

- `state.presets.firstOrNull { ... }`
- プリセットがない場合の `IntArray(0)`
- `resolveMacro()` が返す `Pair<Int, Int>`

ソース上は一時オブジェクトまたは反復子が生じ得る形である。改善するなら、プリミティブ結果を個別に求める、空配列を事前確保する、アクティブプリセット参照を状態変更時に解決する、といった選択肢がある。

### P-02 描画中の動的文字列

根拠例:

- `Scenes.kt:330`
- `Scenes.kt:1356-1374`

警報中の `" $msg "` や、テンプレート編集画面の数値＋単位文字列が描画ごとに構築される。視覚的な不具合ではないが、無割り当て描画を目標とする場合は、数字を字形単位で描く、状態変更時だけ表示バッファを更新する、といった整理対象になる。

### P-03 使用されない演出状態を毎フレーム更新する

根拠:

- `Scenes.kt:117-120`
- `MagicCircleDemoscene.kt:21-87`
- `NestedTimeboxInstrumentRenderer.kt:171`

6個のWaveは毎フレーム更新されるが、対象範囲内で各 `value()` の描画利用は確認されない。現在の可視効果はPerlin揺らぎと、別実装の4点尾である。

この状態は大きな負荷ではない可能性があるが、演出の説明、所有状態、実描画が一致していない。

## 7. モジュール性を上げる場合の選択肢

以下は必須の再設計案ではなく、現在の仕組みを保ったまま責務を明確にする選択肢である。

### M-01 描画矩形と入力矩形を同じ計算結果から使う

現在、HUD、タイマー操作、テンプレートカード、エントロピー画面などで、同じ配置式を描画側と入力側が別々に再計算する。

割り当てを増やさずに行うなら、シーンごとの事前確保された矩形フィールド、またはプリミティブ値を書き込むレイアウト関数を使い、描画と入力で同じ結果を参照できる。

これはB-02の直接原因を除き、今後の片側変更によるずれも減らす。

### M-02 スクロールジェスチャーの状態機械を共有する

根拠:

- `Scenes.kt:433-480`
- `Scenes.kt:1060-1109`
- `Scenes.kt:1415-1460`
- `Scenes.kt:2163-2212`

4シーンにDOWN/MOVE/UP/CANCEL、初期座標、ドラッグ状態、スクロール制限が反復され、閾値も異なる。

事前確保された小さなジェスチャー状態、またはプリミティブ関数へ寄せれば、フレームワークやコレクションを導入せず一貫性を上げられる。

### M-03 整数エイリアス経路とプラットフォーム図形経路を明確に分ける

根拠:

- `AliasedVectorLayer.kt:7-14`
- `ScaledProceduralRenderer.kt:48-62`
- `ScaledProceduralRenderer.kt:317-510`
- `ScaledProceduralRenderer.kt:579-665`

`AliasedVectorLayer` は権威的な整数走査器として説明される一方、現行の星形、目盛、珠などには `canvas.drawLine` や `canvas.drawRect` への直接経路が残る。

選択肢は二つある。

- UI・装飾の線を整数エイリアス層へ通す。
- 直接プラットフォーム経路を意図的に残し、用途、整数化、クリップ、アンチエイリアスの契約を別名で明確にする。

どちらが正しいかは、本書だけでは決定しない。

### M-04 アイコン生成は色役割までを所有する

現在、アイコン形状は `ProceduralMath.getPixelColor`、色変換は `ProceduralIconRenderer.mapIconColor` に分かれ、ARGB値が両者の非公開契約になっている。

形状生成が小さな色役割番号を返し、UI側が役割を現在のパレットへ解決する形なら、外部資産を増やさず、未対応色の取りこぼしも避けられる。

### M-05 シーン別ファイルへの物理分割

`Scenes.kt` は3262行あり、複数シーン、入力変換、配置、スクロール、文字描画補助を同じファイルに持つ。

シーンのシングルトン性や実行モデルを変えず、各シーンを別ファイルへ物理的に分けるだけでも、全文精読、呼び出し追跡、変更範囲の限定は容易になる。ただし、分割自体を目的とした変更ではなく、具体的な作業で必要になった場合だけ検討するのが安全である。

### M-06 時間源の役割を分ける

現在、実時間 `dt` を使うWaveと、更新回数を60Hzとみなす `FrameClock` が並存する。

- 決定論的なフレーム位相
- 実秒に基づくアニメーション

この二つを別の明示的な契約として扱えば、更新頻度による速度差と、1引数版の二重tickの影響範囲を小さくできる。

## 8. 描画境界の契約上の確認事項

### EngineCanvasの座標型

`EngineCanvas.kt:12-23` では `setPixel` までFloat座標を受け取る。`ScaledProceduralRenderer.kt:260-305` の回転字形は、中心が小数なら最終画素座標も小数のまま出力し得る。

各プラットフォーム実装が同じ丸めを行っている可能性はあるが、commonMainの型だけからは次を保証できない。

- 最終整数化の規則
- 矩形の右端・下端が包含か半開か
- Canvas外書き込みの扱い
- ディザの原点と周期
- 線・円のアンチエイリアス状態

### densityの意味

`EngineCanvas.kt:14` は一般的な画面密度として説明する一方、現在のプロジェクトのCanvasモデルでは `density = 1f` が契約である。`ScaledProceduralRenderer.kt:323-352` などは寸法へdensityを乗算する。

現行アダプタが1fを守れば問題は出ない。将来の実装者が一般的なDPI値を設定すると、その図形だけ二重拡大される可能性があるため、「常に1fの互換値」であることを境界に明記する余地がある。

## 9. 優先順位案

実装修正を行う場合の優先順位候補であり、変更の承認ではない。

1. 警報時の入力優先順位を決める。
2. エントロピー画面の描画・入力位置を同じ計算へ揃える。
3. テキストカーソルを実際の挿入位置へ揃える。
4. 魔法陣の10文字重複と演出スイッチの範囲を仕様確認する。
5. アイコンの色役割を欠落なくパレットへ対応させる。
6. 拡大字形と太線の最終クリップを明確にする。
7. 狭い表示と低背表示の配置下限を内容から計算する。
8. Uを使っていないUI寸法を、U式または理由付き部品定数として整理する。
9. 描画と入力の共通レイアウト計算、時間源、未使用演出状態を段階的に整理する。

## 10. 監査の限界

- 対象ディレクトリ外のプラットフォームCanvas実装は監査していない。
- 実機での表示、タッチ、DPI、クリップ、アンチエイリアス挙動は確認していない。
- ビルド、テスト、画素差分、スクリーンショット比較は行っていない。
- 「条件付き」とした所見は、対象外の呼び出し側契約が成立していれば顕在化しない場合がある。
- 数学的・構造的に確認できる問題と、意匠上の選択は分離した。特に手続き的図形の局所座標や角度定数は、UI配置のU法則と同一視していない。
